<div class="container" style="margin-top: 20px">
  <div class="row">
      <div class="col-6 offset-3">
        {{#if error}}
            <h6 class="message-campos">{{message}}</h6>
    	{{/if}}
        <form action="/admin/novedades/agregar" method="post" enctype="multipart/form-data">
        <div class="mb-3">
            <label for="usuario" class="form-label titulo-agregar">Título</label>
            <input type="text" class="form-control agregar-titulo" id="usuario" name="titulo" />
        </div>
        <div class="mb-3">
            <label for="password" class="form-label subtitulo-agregar">Subtitulo</label>
            <textarea class="form-control agregar-subtitulo" name="subtitulo"></textarea>
        </div>

        <div class="mb-3">
            <label class="form-label imagen-agregar">Imagen</label>
            <input type="file" name="imagen" class="input-agregar"/>
        </div>

        <div class="mb-3">
            <label for="password" class="form-label cuerpo-agregar">Cuerpo</label>
            <textarea class="form-control agregar-cuerpo" name="cuerpo"></textarea>
        </div>
        <div class="btn-agregar">
            <button type="submit" class="btn btn-primary">Agregar</button>
        </div>
        </form>
      </div>
  </div>
</div>